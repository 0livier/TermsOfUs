# Tickets

Tickets are small reviewable slices. The code in `src/` is the source of truth for what is currently implemented.

Only open tickets remain in this directory. Completed and partially completed historical tickets have been removed to keep the backlog actionable.

## Open tickets

| Ticket | Status | Notes |
| --- | --- | --- |
| 007 | Open | Sunburst SVG components exist in `src/sunburst/`, but the desktop visual map is not wired into the primary app. Decide whether to implement it or remove the unused components. |
| 009 | Open | Accessibility pass still needs verification against the current category-card UI. |
